[engine.asr]
provider = "faster-whisper"
model_name = "small"
device = "cpu"
compute_type = "int8"
bundled_models = ["small", "medium"]
allow_cpu_fallback = true

[task]
max_concurrency = 2
max_heavy_concurrency = 1
max_retries = 2
